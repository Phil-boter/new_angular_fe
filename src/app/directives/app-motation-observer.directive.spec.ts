import { AppMotationObserverDirective } from './app-motation-observer.directive';

describe('AppMotationObserverDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMotationObserverDirective();
    expect(directive).toBeTruthy();
  });
});
